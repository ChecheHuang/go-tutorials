apiVersion: v1
kind: Service
metadata:
  name: blog-api
  namespace: blog
  labels:
    app: blog-api
spec:
  type: ClusterIP
  selector:
    app: blog-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
